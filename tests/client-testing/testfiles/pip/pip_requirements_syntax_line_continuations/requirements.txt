# Test line continuations (backslash syntax)
# Line continuations allow splitting long package specifications across multiple lines
# This syntax is commonly used with hashes, URLs, and complex version specifiers

# Simple line continuation with environment marker
requests==2.25.1 ; \
    python_version >= "3.6"

# Multiple version specifier continuations
urllib3>=1.26.0, \
    <2.0.0

# Line continuation with extras
certifi==2021.5.30

# Multiple continuations on version range
click>=7.0, \
    <9.0

