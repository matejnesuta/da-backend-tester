[project]
name = "test-project"
version = "0.1.0"
description = "Test uv consuming package extras syntax (PEP 508)"
dependencies = [
    "requests[security,socks]==2.25.1",
    "celery[redis]==5.2.7",
    "httpx[http2]>=0.23.0",
    "flask==2.0.3",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
