[project]
name = "test-project"
version = "0.1.0"
description = "Test uv with optional dependencies (should be analyzed)"
dependencies = [
    "flask==2.0.3",
    "requests==2.25.1",
]

[project.optional-dependencies]
security = [
    "cryptography==3.4.8",
    "pyOpenSSL==21.0.0",
]
extras = [
    "uvicorn==0.17.0", #exhortignore
    "click==8.0.4",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
