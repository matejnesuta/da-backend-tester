[project]
name = "package-a"
version = "0.1.0"
description = "Package A with vulnerable dependencies"
requires-python = ">=3.11"
dependencies = [
    "flask==2.0.3",
    "requests==2.25.1",
    "package-b",
]

[tool.uv.sources]
package-b = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
