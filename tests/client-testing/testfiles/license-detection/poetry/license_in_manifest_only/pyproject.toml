[tool.poetry]
name = "license-manifest-only"
version = "0.1.0"
description = ""
authors = ["Test <test@example.com>"]
license = "Apache-2.0"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
