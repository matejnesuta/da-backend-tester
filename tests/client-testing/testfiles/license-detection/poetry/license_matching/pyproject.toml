[tool.poetry]
name = "license-matching"
version = "0.1.0"
description = ""
authors = ["Test <test@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
