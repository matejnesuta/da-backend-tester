[project]
name = "license-matching"
version = "0.1.0"
description = ""
license = {text = "MIT"}
dependencies = [
    "requests>=2.28.0",
]
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
